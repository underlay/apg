export * from "./utils.js";
export * from "./apply.js";
export const identifier = (value) => Object.freeze({ type: "identifier", value });
export const constant = (value, datatype) => Object.freeze({ type: "constant", value, datatype });
export const dereference = (key) => Object.freeze({ type: "dereference", key });
export const projection = (key) => Object.freeze({ type: "projection", key });
export const injection = (key) => Object.freeze({ type: "injection", key });
export const tuple = (slots) => Object.freeze({ type: "tuple", slots: Object.freeze(slots) });
export const match = (cases) => Object.freeze({ type: "match", cases: Object.freeze(cases) });
export const map = (source, value) => Object.freeze({ type: "map", source, value });
export const mapping = (maps) => Object.freeze(maps);
